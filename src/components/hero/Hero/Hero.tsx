
import Nav from '../Nav/Nav.tsx';
import HeroBody from './HeroBody/HeroBody.tsx';

export default function Hero() {
    return (
        <header id="hero">
            <Nav />
            <HeroBody />
        </header>
    );
}